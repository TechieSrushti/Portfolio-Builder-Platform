{
  "name": "portfolio-builder-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --legacy-peer-deps && npm install --prefix portfolio-builder --legacy-peer-deps && npm install --prefix server --legacy-peer-deps",
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix portfolio-builder",
    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
